import React from 'react'
import { Text, View } from 'react-native'
import {styles} from '../theme/appTheme'

export const Secciones = () => {
  return (
    <View>
        <Text style={styles.title}>Seccion Secciones</Text>
    </View>
  )
}
